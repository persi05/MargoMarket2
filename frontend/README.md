# MargoMarket Frontend

Prosta aplikacja Angular konsumująca API Spring Boot z tego repozytorium.

## Uruchomienie

```bash
cd frontend
npm install
npm start
```

Frontend działa domyślnie pod `http://localhost:4200` i używa proxy `/api -> http://localhost:8080`.
